<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="flow_desktop.UI.Components.SeekBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:flow_desktop.UI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Slider
            x:Name="SeekBarSlider"
            Minimum="0"
            Maximum="1"
            StepFrequency="0.01"
            IsTabStop="False"
            Value="{x:Bind PlayProgress, Mode=OneWay}"
            Width="{x:Bind SeekBarWidth, Mode=OneWay}"
            >
        <Slider.Resources>
            <!-- inactive track colors -->
            <SolidColorBrush x:Key="SliderTrackFill" Color="{StaticResource ColorAguero}" />
            <SolidColorBrush x:Key="SliderTrackFillPointerOver" Color="{StaticResource ColorAguero}" />
            <SolidColorBrush x:Key="SliderTrackFillPressed" Color="{StaticResource ColorAguero}" />


            <!-- active track colors -->
            <SolidColorBrush x:Key="SliderTrackValueFill" Color="{StaticResource ColorTelli}" />
            <SolidColorBrush x:Key="SliderTrackValueFillPointerOver" Color="{StaticResource ColorTelli}" />
            <SolidColorBrush x:Key="SliderTrackValueFillPressed" Color="{StaticResource ColorTelli}" />

            <SolidColorBrush x:Key="SliderThumbBackground" Color="{StaticResource ColorTelli}" />
            <SolidColorBrush x:Key="SliderThumbBackgroundPointerOver" Color="{StaticResource ColorTelli}" />
            <SolidColorBrush x:Key="SliderThumbBackgroundPressed" Color="{StaticResource ColorTelli}" />

        </Slider.Resources>
    </Slider>
</UserControl>
