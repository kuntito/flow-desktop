<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="flow_desktop.UI.Components.AlbumArtSongInfoSP"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:flow_desktop.UI.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:components="using:flow_desktop.UI.Components"
    mc:Ignorable="d">

    <StackPanel
        VerticalAlignment="Center"
        >
        <components:AlbumArtSP />
        <Border Height="16" />
        <components:SongTitleAndArtistSP
            SongTitle="{x:Bind SongTitle, Mode=OneWay}"
            ArtistStr="{x:Bind ArtistStr, Mode=OneWay}"
            />
    </StackPanel>
</UserControl>
